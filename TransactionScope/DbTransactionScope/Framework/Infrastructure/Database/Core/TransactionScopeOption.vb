''' <summary>
''' トランザクションスコープがどのようにトランザクションに参加するかを指定します。
''' </summary>
Public Enum TransactionScopeOption

    ''' <summary>
    ''' 既存のトランザクションが存在する場合はそれに参加し、
    ''' 存在しない場合は新しいトランザクションを開始します。
    ''' 通常のユースケースではこれを使用します。
    ''' </summary>
    Required = 0

    ''' <summary>
    ''' 常に新しいトランザクションを開始します。
    ''' 既存のトランザクションがあっても参加しません。
    ''' 既存トランザクションとは独立した処理を行いたい場合に使用します。
    ''' </summary>
    RequiresNew = 1

    ''' <summary>
    ''' トランザクションに参加しません。
    ''' 既存のトランザクションがあっても無視し、非トランザクションとして実行します。
    ''' ログ書き込みなど、トランザクションに巻き込まれたくない処理に使用します。
    ''' </summary>
    Suppress = 2

End Enum